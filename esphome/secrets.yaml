# ESPHome secrets — keep this file out of version control if you push publicly.
# Referenced as !secret <key> in aquaclean-proxy-*.yaml.

# WiFi credentials (used by aquaclean-proxy-wifi.yaml)
wifi_ssid: "your-wifi-ssid"
wifi_password: "your-wifi-password"

# Generate the API encryption key with:  openssl rand -base64 32
# Then uncomment the api: encryption: block in the proxy YAML.
# api_encryption_key: "your-base64-key"

ota_password: "change-me"
